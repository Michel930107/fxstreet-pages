# FXStreet Data Pages

A GitHub Pages catalog for small, self-contained market-data pages.

## Structure

- `index.html` — catalog landing page.
- `pages/usdinr.html` — USD/INR symbol results.

Add future pages under `pages/`, then add a card to `index.html`.
